name: rampage_html

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  meta: ^1.16.0
  web: ^1.1.0

dev_dependencies:
  lints: ^5.1.1
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.4
